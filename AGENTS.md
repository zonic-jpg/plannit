# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield repository called **plannit** (a planning application). As of the initial setup, the repo contains only a `README.md` with no source code, dependencies, or services.

- **No build system, package manager, or dependencies** are configured yet.
- **No linting, testing, or CI/CD** pipelines exist.
- **No services** need to be started.

When code is added to this repo, update this file with:
- The package manager and install command (e.g. `pnpm install`)
- How to run the dev server, tests, and linter
- Any non-obvious environment setup steps or gotchas
